DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
DIRECT_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme

# MQTT for browser
NEXT_PUBLIC_MQTT_URL=ws://localhost:9001/mqtt
NEXT_PUBLIC_MQTT_BASE=smb

# Optional server-side MQTT (for /api/mqtt/command)
MQTT_TCP_HOST=localhost
MQTT_TCP_PORT=1883

# SSE/Webhook secrets
WEBHOOK_SECRET=dev-shared-webhook-secret
WS_SHARED_SECRET=dev-ws-secret

# Camera stream (MJPEG) default; can be overridden in UI localStorage
NEXT_PUBLIC_PI_STREAM=http://raspberrypi.local:8080/stream.mjpg
